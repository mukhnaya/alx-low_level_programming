#include <stdio.h>
/**
 * main - function as entry
 * @argc: to return number of arguments
 * @argv: to return strings
 *
 * Return: 0
 */
int main(__attribute__((unused)) int argc, char *argv[])
{
	int pau;

	pau = 0;
	printf("%s \n", argv[pau]);
	return (0);
}
