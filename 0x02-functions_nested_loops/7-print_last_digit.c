#include "main.h"
/**
 * print_last_digit - prints last digit
 *
 * @number: is the last digit
 *
 * Returns: last value
 */
int print_last_digit(int number)
{
	int last;

	last = number % 10;
	return (last);
}
